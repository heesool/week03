[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "hello-pkg-25170001043"
version = "0.1.0"
description = "A simple package for t02"
requires-python = ">=3.10"

[tool.setuptools.packages.find]
where = ["src"]
